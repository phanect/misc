import type { Config } from "prettier";

const config: Config = {
  bracketSameLine: false,
  bracketSpacing: true,
  proseWrap: "preserve",
  semi: true,
  singleQuote: false,
  tabWidth: 2,
  trailingComma: "all",
};

export default config;
